# Whitelist strategy: ignore EVERYTHING, then re-include only the shipped payload.
# Any new dev artifact is excluded from the .vsix by default.
**

# --- shipped payload ---
!package.json
!README.md
!CHANGELOG.md
!LICENSE
!images/
!themes/pixel-berry-color-theme.json
